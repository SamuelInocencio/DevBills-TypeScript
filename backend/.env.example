PORT=3001
NODE_ENV=dev

# MongoDB — o Prisma exige um replica set (use MongoDB Atlas ou um replica set local)
DATABASE_URL=

# Firebase Admin (Service Account)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
